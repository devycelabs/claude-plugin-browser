{
  "plugin-browser": {
    "command": "node",
    "args": ["${CLAUDE_PLUGIN_ROOT}/server/index.js"],
    "env": {
      "PLUGIN_BROWSER_PORT": "${PLUGIN_BROWSER_PORT}",
      "PLUGIN_BROWSER_URL":  "${PLUGIN_BROWSER_URL}",
      "PLUGIN_BROWSER_DEV":  "${PLUGIN_BROWSER_DEV}"
    }
  }
}
